import React from "react";

function Loading() {
  return (
    <div
      style={{
        marginTop: "20%",
        textAlign: "center",
        fontSize: 20
      }}
    >
      <i className="fas fa-spinner"></i>
    </div>
  );
}

export default Loading;
